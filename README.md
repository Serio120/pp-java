# pp-java

  ```bash
  git push -u origin main
```

```bash
  git pull origin main --rebase
```

```bash
  git add .
```

```bash
  git commit -m "Guardando cambios antes del rebase"
```

```bash
  git pull origin main --rebase
```

```bash
  git status
```

```bash
  git push -u origin main
```
